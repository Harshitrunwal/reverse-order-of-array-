#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n; // Read the number of integers in the array

    int arr[n]; // Declare an array of size n

    // Read the array elements
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    // Print the array elements in reverse order
    for (int i = n - 1; i >= 0; i--) {
        cout << arr[i];
        if (i > 0) {
            cout << " "; // Print a space between elements (except after the last element)
        }
    }

    cout << endl;

    return 0;
}
